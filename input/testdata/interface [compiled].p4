extern Crc16<T> {
  <null> Crc16<>();
  <null> Crc16<>(in int<32> x);
  void initialize<U>(in U input_data);
  T value<>();
  T make_index<>(in T size, in T base); }
control p() { }
control empty<>();
package m<>( empty e);
m main(p())
